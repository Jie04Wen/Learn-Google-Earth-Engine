# GPT × GEE 提示词模板

把方括号内容替换为你的真实信息；不要上传账号口令、API Key、未获授权的数据或精确敏感位置。

## 1. 从零生成最小可运行代码

你是一名熟悉 Google Earth Engine JavaScript Code Editor 的遥感工程师。

任务目标：[用一句话描述科学问题]。

输入：
- 数据集：[准确的 Earth Engine Data Catalog ID]
- ROI：[绘制几何 / projects/.../assets/...]
- 日期：[开始日期，结束日期；说明结束日期是否包含]
- 关键波段与单位：[列出]

处理要求：
1. [云掩膜方法]
2. [缩放因子]
3. [指数/合成/统计方法]
4. [scale、CRS 或分区字段]

输出：
- 地图图层：[名称、min/max、palette]
- Console：[需要打印的对象、数量、范围]
- 图表/导出：[类型、格式、字段]

约束：仅使用 GEE Code Editor JavaScript API；先给一个小 ROI、短日期的最小可运行版本；逐行中文注释；不要编造数据集 ID、波段或 API；不确定处明确标记并给出 Earth Engine 官方文档核验链接。

验收标准：[集合 size > 0；目标波段存在；数值范围；图表；随机样点；导出参数]。

## 2. 精准调试

下面代码在 GEE Code Editor 报错。请先判断它属于语法错误、对象类型错误、空集合、权限、资源限制，还是科学逻辑错误；只修改导致问题的最少代码。

完整错误信息：[原样粘贴]

最小复现代码：[粘贴 20—60 行相关代码]

预期结果：[具体说明]

我已检查：[数据 ID / 日期 / ROI / bandNames / size / 权限]。

请返回：根因、修改前后差异、可复制代码、运行后应检查的 3 个证据。若缺少必要信息，先提出最多 3 个具体问题。

## 3. 代码审查与科研验收

请审查下面的 GEE 代码，不要只做语法检查。按严重度列出：
1. 数据集 ID、波段、比例因子和 QA 位是否与官方目录一致；
2. 时间窗口、结束日期、掩膜传播、投影与 scale 是否一致；
3. 是否存在空集合、first() 为 null、属性丢失或客户端/服务器混用；
4. 统计分母、单位和阈值是否定义清楚；
5. 给出最小验证代码：size、bandNames、min/max、分位数、直方图、随机样点和敏感性分析。

代码：[粘贴]

不要凭印象断言正确；把“已由官方文档确认”“需要在我的账号运行确认”“需要独立数据科学验证”分开写。

## 4. 改为个人 Asset ROI

把代码中的绘制 ROI 改为个人 Table Asset：`[projects/.../assets/...]`。

资产中用于筛选的字段是 `[字段名]`，目标值是 `[字段值]`。请保留 FeatureCollection 属性以便 `reduceRegions` 分区统计，同时创建 `roiFc.geometry()` 供 `filterBounds`、`clip` 和地图定位使用。先打印 `size()`、`first()` 和属性名；如果字段不存在，不要静默返回空集合。

## 5. 改造成公开 Earth Engine App

把下面分析脚本改造成 Earth Engine App。受众是没有 GEE 账号的普通读者。界面需要：标题、方法说明、年份选择、图层更新、图例、状态提示；默认数据必须公开可读。请同时给出发布前清单：Cloud 项目、App 访问方式、Asset 权限、数据许可、隐私、运行配额、移动端可读性和方法限制。

原始代码：[粘贴]
